# AGENTS.md

本文件约束在本仓库中工作的 AI 编码代理。除非用户明确覆盖，以下规则适用于整个仓库。

## 项目目标

- 本项目用于学习和验证 Kubernetes、Ray、KubeRay、vLLM 与分布式训练的调度和生产化实践。
- 优先产出可运行、可观测、可复现实验，不堆砌只展示 API 形状的伪代码。
- 修改前先阅读 `README.md` 和 `ray-production-learning-roadmap.md`，确保实现服务于当前学习路线。

## 工作方式

- 先检查现有目录、依赖和未提交改动，再开始编辑。
- 保持改动聚焦；不要顺手重构与任务无关的文件。
- 不覆盖、不回退用户已有的未提交改动。
- 对版本敏感的 Ray、Kubernetes、KubeRay、vLLM 和 PyTorch API，优先核对官方文档。
- 新增依赖必须说明用途，并选择满足需求的最小依赖范围。

## 语言与风格

- 文档、注释和用户可见输出默认使用中文。
- 文件名、Python 标识符、命令和配置字段使用英文。
- Python 代码遵循 PEP 8，优先使用类型标注和清晰的函数名。
- 注释只解释设计原因、边界条件和不直观行为，不复述代码。
- 示例应短小直接，避免为了复用引入会掩盖核心概念的抽象。

## 目录约定

- `example/`：按全局序号排列、可依次执行的学习任务。
- 后续新增生产项目时，源码、测试、部署清单和文档应分别放入职责明确的目录。
- 不在仓库中提交模型权重、数据集、密钥、集群凭据、日志或大型生成物。

## Example 约束

- `example/` 下每个子目录只演示一个功能或概念。
- 每个任务目录必须包含说明该任务的 `README.md`。
- 尚未实施的任务可以只有 `README.md`；开始实施后，在同一目录补充源码、配置、测试和结果。
- Python 示例统一使用 `main.py` 作为入口；其他语言和部署任务使用符合其生态的标准文件名。
- 每个示例必须能够单独运行，不得导入其他示例目录中的文件。
- 目录名使用两位数字全局排序和功能名，例如 `18_ray_remote_task/`。
- 每个 Python 示例必须包含 `main()` 和 `if __name__ == "__main__":` 入口。
- 本地示例必须显式调用 `ray.init()`，并在 `finally` 中调用 `ray.shutdown()`。
- 示例输出应稳定、简短，并通过 `assert` 验证关键结果。
- 示例不得依赖公网、真实 Kubernetes 集群、GPU 或云资源，除非文件名和文档明确标注该前置条件。
- 一个目录需要讲解第二个独立概念时，应拆成新的任务目录。
- 完成任务后，在 `example/README.md` 中更新状态，不提前标记未验收任务。

## 分布式与生产安全

- Ray 的 CPU、GPU 和自定义资源均按逻辑资源处理，不把资源声明描述成操作系统级隔离。
- 故障恢复示例必须说明任务可能重试、Actor 状态可能丢失，以及操作是否需要幂等。
- Kubernetes 清单必须显式配置适当的 requests/limits，并避免默认暴露 Dashboard 或管理端口。
- 不执行针对真实集群的删除、驱逐、扩缩容、回滚或故障注入，除非用户明确要求并确认目标环境。
- 不在代码或清单中写入 token、cookie、kubeconfig、云密钥或内部地址。

## 验证要求

- 修改 Python 文件后，至少运行静态编译检查。
- 环境具备依赖时，运行受影响的示例或测试，并检查退出码和关键输出。
- 涉及并发、重试、调度或故障恢复时，测试成功路径和至少一个边界或失败路径。
- 无法运行验证时，明确记录缺少的依赖、硬件或集群条件，不声称已经通过。
- 完成后检查 `git diff`，确认没有无关改动和生成物。

## 文档维护

- 新增任务时，为其编写 `README.md`，并同步更新 `example/README.md` 的路线索引。
- 命令应可直接复制执行，并注明工作目录和必要前置条件。
- 架构结论应区分 Kubernetes Scheduler、Ray Scheduler 和模型 Runtime 三层职责。
- 不静默修改学习路线中的目标、周期或验收标准；需要调整时说明原因。
